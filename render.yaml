services:
  - type: web
    name: zorvyn-backend
    env: python
    buildCommand: "pip install -r requirements.txt && pip install pydantic[email]"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"